// packages/infra — SST infrastructure definitions
// All AWS resources (DynamoDB tables, Lambda functions, API Gateway, SQS) are defined here.
// Resources added in Issue 2 (DynamoDB) and Issue 4 (Lambda + API Gateway).
export {};
